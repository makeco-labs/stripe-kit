{
  "name": "@makeco/stripe-kit",
  "version": "0.1.0",
  "author": "makeco",
  "repository": {
    "type": "git",
    "url": "https://github.com/makeco/stripe-kit.git"
  },
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "stripe-kit": "./dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.js"
    }
  },
  "bugs": {
    "url": "https://github.com/makeco/stripe-kit/issues"
  },
  "description": "An unofficial CLI tool for creating, archiving, updating Stripe products and prices and syncing them to your database.",
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "package.json"
  ],
  "homepage": "https://github.com/makeco/stripe-kit",
  "keywords": [
    "stripe",
    "billing",
    "subscription",
    "cli",
    "payments",
    "typescript",
    "saas"
  ],
  "license": "MIT",
  "private": false,
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "bun clean && tsc -p tsconfig.build.json && tsc-alias -p tsconfig.build.json --resolve-full-paths && chmod +x dist/cli.js",
    "dev": "tsc -p tsconfig.build.json --watch",
    "dev:typecheck": "bun typecheck --watch --preserveWatchOutput",
    "lint": "npx ultracite@latest lint",
    "format": "bunx ultracite@latest format",
    "test": "vitest",
    "ci": "bun lint && bun typecheck && bun format && bun run build",
    "git": "bun ci && git add . && git commit -m",
    "postgit": "git push --all"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/prompts": "^2.4.9",
    "eslint": "^9.29.0",
    "tsc-alias": "^1.8.16",
    "typescript": "^5.8.3",
    "ultracite": "^5.1.2",
    "vitest": "^3.2.4"
  },
  "type": "module",
  "dependencies": {
    "chalk": "^5.2.0",
    "commander": "^12.1.0",
    "dotenv": "^16.0.3",
    "esbuild-register": "^3.6.0",
    "prompts": "^2.4.2",
    "stripe": "^18.4.0",
    "zod": "^4.0.14"
  },
  "peerDependencies": {
    "drizzle-orm": ">=0.40.0"
  }
}
