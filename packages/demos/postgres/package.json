{
  "name": "@demo/postgres",
  "version": "1.0.0",
  "description": "Example project using stripe-kit",
  "type": "module",
  "scripts": {
    "stripe": "stripe-kit -c stripe.config.ts",
    "db:drop": "dotenv -e ./.env.dev drizzle-kit drop",
    "db": "db -c db.config.ts"
  },
  "dependencies": {
    "@makeco/db-cli": "^0.1.2",
    "@makeco/stripe-kit": "workspace:*",
    "drizzle-orm": "^0.44.4",
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "@types/pg": "^8.10.0",
    "dotenv-cli": "^10.0.0",
    "drizzle-kit": "^0.31.4",
    "typescript": "^5.0.0"
  }
}
