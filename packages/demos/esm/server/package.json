{
  "name": "@demo/esm-server",
  "version": "1.0.0", 
  "description": "ESM server package for testing stripe-kit config loading",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "stripe": "stripe-kit -c stripe.config.ts"
  },
  "dependencies": {
    "@demo/esm-db": "workspace:*",
    "@makeco/stripe-kit": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}